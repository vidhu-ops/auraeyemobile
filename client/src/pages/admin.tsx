import AdminCrmApp from "./admin-crm/AdminCrmApp";

/** AuraEye Admin CRM — Phase 1 operational dashboard (replaces the minimal healer tools panel). */
export default function AdminPage() {
  return <AdminCrmApp />;
}
